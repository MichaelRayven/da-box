import { SignUpDialog } from "./dialog";
export default async function SignUpPage() {
  return (
    <section className="md:-mt-32 flex w-full items-center justify-center md:h-screen">
      <SignUpDialog />
    </section>
  );
}
